
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>MathVerse - 高中数学学习宇宙</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    /* 图标按钮 */
    .icon-btn {
      background: none;
      border: none;
      color: var(--text-light);
      font-size: 22px;
      cursor: pointer;
      padding: 8px;
      transition: all 0.3s;
    }

    .icon-btn:hover {
      color: var(--accent);
      transform: scale(1.1);
    }

    /* 模态框基础样式 */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.7);
      backdrop-filter: blur(5px);
    }

    .modal-content {
      background: var(--bg-card);
      margin: 10% auto;
      padding: 40px;
      width: 90%;
      max-width: 400px;
      border-radius: 20px;
      box-shadow: 0 16px 32px rgba(0, 0, 0, 0.5);
      text-align: center;
    }

    .close {
      color: var(--text-light);
      float: right;
      font-size: 28px;
      font-weight: bold;
      cursor: pointer;
      position: absolute;
      top: 16px;
      right: 16px;
    }

    .close:hover {
      color: var(--accent);
    }

    /* 表单样式 */
    .modal input[type="text"],
    .modal input[type="email"],
    .modal input[type="password"] {
      width: 100%;
      padding: 12px;
      margin: 12px 0;
      border: 1px solid #445577;
      border-radius: 10px;
      background: rgba(255,255,255,0.05);
      color: white;
      font-size: 1rem;
    }

    .modal button {
      background: var(--accent);
      color: var(--bg-dark);
      border: none;
      padding: 12px 24px;
      border-radius: 50px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s;
    }

    .modal button:hover {
      background: #00d0b3;
      transform: scale(1.05);
    }

    .modal a {
      color: var(--accent);
      text-decoration: none;
    }

    .modal a:hover {
      text-decoration: underline;
    }

    :root {
      --bg-dark: #0F1A2C;
      --bg-card: #1A2B47;
      --accent: #00F5D4;
      --accent-glow: rgba(0, 245, 212, 0.7);
      --text: #E0E0FF;
      --text-light: #A0B0D0;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'HarmonyOS Sans SC', 'Noto Sans SC', sans-serif;
    }

    body {
      background: var(--bg-dark);
      color: var(--text);
      min-height: 100vh;
      background-image: 
        radial-gradient(circle at 10% 20%, rgba(0, 245, 212, 0.08) 0%, transparent 20%),
        radial-gradient(circle at 90% 80%, rgba(0, 150, 255, 0.1) 0%, transparent 25%);
      overflow-x: hidden;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    /* 星星容器 - 固定在最底层 */
    .stars-container {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      pointer-events: none;
      z-index: -1;
      overflow: hidden;
    }

    /* 启明星样式 - 最大最亮的那颗 */
    .morning-star {
      position: absolute;
      top: 15%;
      left: 15%;
      width: 12px;
      height: 12px;
      background: linear-gradient(135deg, #ffffff, #00F5D4);
      border-radius: 50%;
      box-shadow: 
        0 0 30px #ffffff,
        0 0 60px rgba(0, 245, 212, 0.7),
        0 0 100px rgba(0, 245, 212, 0.4);
      animation: morningStarPulse 3s ease-in-out infinite alternate;
    }

    @keyframes morningStarPulse {
      0% {
        transform: scale(1);
        box-shadow: 
          0 0 20px #ffffff,
          0 0 40px rgba(0, 245, 212, 0.6),
          0 0 80px rgba(0, 245, 212, 0.3);
      }
      100% {
        transform: scale(1.3);
        box-shadow: 
          0 0 40px #ffffff,
          0 0 80px rgba(0, 245, 212, 0.8),
          0 0 140px rgba(0, 245, 212, 0.5);
      }
    }

    /* 大星星 */
    .star-big {
      position: absolute;
      width: 8px;
      height: 8px;
      background: white;
      border-radius: 50%;
      animation: starTwinkleBig 2s infinite alternate;
    }

    @keyframes starTwinkleBig {
      0%, 100% {
        opacity: 0.3;
        transform: scale(1);
        box-shadow: 
          0 0 5px white,
          0 0 10px rgba(0, 245, 212, 0.5);
      }
      50% {
        opacity: 1;
        transform: scale(1.2);
        box-shadow: 
          0 0 15px white,
          0 0 30px rgba(0, 245, 212, 0.8);
      }
    }

    /* 中星星 */
    .star-medium {
      position: absolute;
      width: 5px;
      height: 5px;
      background: white;
      border-radius: 50%;
      animation: starTwinkleMedium 1.8s infinite alternate;
    }

    @keyframes starTwinkleMedium {
      0%, 100% {
        opacity: 0.2;
        transform: scale(1);
        box-shadow: 0 0 3px white;
      }
      50% {
        opacity: 0.8;
        transform: scale(1.1);
        box-shadow: 0 0 8px white, 0 0 16px rgba(0, 245, 212, 0.6);
      }
    }

    /* 小星星 */
    .star-small {
      position: absolute;
      width: 3px;
      height: 3px;
      background: white;
      border-radius: 50%;
      animation: starTwinkleSmall 1.5s infinite alternate;
    }

    @keyframes starTwinkleSmall {
      0%, 100% {
        opacity: 0.1;
        transform: scale(1);
      }
      50% {
        opacity: 0.6;
        transform: scale(1.05);
      }
    }

    /* 彩色星星 */
    .star-color {
      position: absolute;
      width: 6px;
      height: 6px;
      border-radius: 50%;
      animation: starTwinkleColor 2.5s infinite alternate;
    }

    .star-color.blue {
      background: linear-gradient(135deg, #00F5D4, #00C3FF);
      box-shadow: 0 0 10px #00F5D4;
    }

    .star-color.purple {
      background: linear-gradient(135deg, #9D4EDD, #C77DFF);
      box-shadow: 0 0 10px #9D4EDD;
    }

    .star-color.pink {
      background: linear-gradient(135deg, #FF6B9D, #FF9EBC);
      box-shadow: 0 0 10px #FF6B9D;
    }

    @keyframes starTwinkleColor {
      0%, 100% {
        opacity: 0.3;
        transform: scale(1);
      }
      50% {
        opacity: 1;
        transform: scale(1.2);
      }
    }

    /* 流星效果 */
    .shooting-star {
      position: absolute;
      width: 2px;
      height: 40px;
      background: linear-gradient(to right, transparent, white);
      opacity: 0;
      transform: rotate(45deg);
    }

    @keyframes shootStar {
      0% {
        opacity: 0;
        transform: translateX(0) translateY(0) rotate(45deg);
      }
      10% {
        opacity: 1;
      }
      100% {
        opacity: 0;
        transform: translateX(400px) translateY(400px) rotate(45deg);
      }
    }

    .container {
      max-width: 1600px;
      margin: 0 auto;
      padding: 0 40px;
      flex: 1;
      width: 100%;
      position: relative;
      z-index: 1;
    }

    /* ============ 头部导航 ============ */
    header {
      padding: 20px 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: relative;
      z-index: 2;
    }

    .logo {
      font-size: 34px;
      font-weight: 800;
      background: linear-gradient(90deg, #00F5D4, #00C3FF);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      letter-spacing: -0.5px;
      text-decoration: none;
      transition: all 0.3s ease;
    }

    .logo:hover {
      transform: scale(1.05);
      text-shadow: 0 0 20px rgba(0, 245, 212, 0.5);
    }

    nav ul {
      display: flex;
      gap: 30px;
      list-style: none;
    }

    nav a {
      font-weight: 600;
      position: relative;
      padding: 6px 0;
      transition: all 0.3s;
      color: var(--text);
      text-decoration: none;
      font-size: 1.2rem;
    }

    nav a:hover {
      color: var(--accent);
    }

    nav a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 0;
      height: 3px;
      background: var(--accent);
      transition: width 0.3s ease;
      border-radius: 2px;
    }

    nav a:hover::after {
      width: 100%;
    }

    .icons {
      display: flex;
      gap: 20px;
      font-size: 24px;
      color: var(--text-light);
    }

    .icons a {
      color: var(--text-light);
      transition: all 0.3s ease;
      padding: 8px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .icons a:hover {
      color: var(--accent);
      background: rgba(0, 245, 212, 0.1);
      transform: translateY(-3px);
    }

    /* ============ 主内容区 ============ */
    main {
      padding: 40px 0 60px;
      flex: 1;
      position: relative;
      z-index: 1;
    }

    .hero {
      text-align: center;
      margin-bottom: 60px;
      padding: 40px 0;
    }

    .hero h1 {
      font-size: 3rem;
      margin-bottom: 20px;
      background: linear-gradient(90deg, #ffffff, #a0d2ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      line-height: 1.2;
    }

    .hero p {
      font-size: 1.3rem;
      color: var(--text-light);
      max-width: 900px;
      margin: 0 auto;
      line-height: 1.6;
    }

    .hero p:last-of-type {
      margin-top: 15px;
      font-weight: 600;
      color: var(--accent);
    }

    /* ============ 卡片网格 ============ */
    .cards {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 25px;
      margin-bottom: 60px;
    }

    .card {
      background: linear-gradient(145deg, rgba(26, 43, 71, 0.9), rgba(20, 35, 60, 0.85));
      border-radius: 20px;
      padding: 30px;
      transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      cursor: pointer;
      border: 2px solid rgba(0, 245, 212, 0.15);
      text-decoration: none;
      color: inherit;
      display: flex;
      flex-direction: column;
      height: 100%;
      position: relative;
      overflow: hidden;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 35px rgba(0, 245, 212, 0.25);
      border-color: rgba(0, 245, 212, 0.4);
    }

    .card-icon {
      font-size: 42px;
      color: var(--accent);
      margin-bottom: 22px;
      transition: transform 0.3s ease;
    }

    .card:hover .card-icon {
      transform: scale(1.1);
    }

    .card h3 {
      font-size: 1.6rem;
      margin-bottom: 15px;
      font-weight: 700;
      background: linear-gradient(90deg, var(--text), #b0c4ff);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
    }

    .card p {
      font-size: 1rem;
      color: var(--text-light);
      line-height: 1.5;
      flex: 1;
    }

    /* ============ 快捷入口 ============ */
    .quick-access {
      margin-top: 60px;
      text-align: center;
    }

    .quick-access h2 {
      text-align: center;
      margin-bottom: 35px;
      font-size: 2rem;
      position: relative;
      display: inline-block;
      color: var(--accent);
    }

    .quick-access h2::after {
      content: '';
      position: absolute;
      bottom: -12px;
      left: 50%;
      transform: translateX(-50%);
      width: 100px;
      height: 4px;
      background: linear-gradient(90deg, var(--accent), #00C3FF);
      border-radius: 2px;
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 25px;
      flex-wrap: wrap;
    }

    .btn {
      background: rgba(0, 245, 212, 0.12);
      color: var(--accent);
      border: 2px solid var(--accent);
      padding: 16px 35px;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1.1rem;
      transition: all 0.3s ease;
      text-decoration: none;
      display: flex;
      align-items: center;
      gap: 12px;
      min-width: 220px;
      justify-content: center;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
    }

    .btn:hover {
      background: linear-gradient(90deg, var(--accent), #00C3FF);
      color: var(--bg-dark);
      box-shadow: 0 12px 25px rgba(0, 245, 212, 0.5);
      transform: translateY(-5px);
    }

    /* ============ 页脚 ============ */
    footer {
      background: rgba(8, 18, 32, 0.95);
      padding: 40px 0 25px;
      border-top: 1px solid rgba(0, 245, 212, 0.2);
      margin-top: auto;
      position: relative;
    }

    footer::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 3px;
      background: linear-gradient(90deg, var(--accent), #00C3FF);
    }

    .footer-container {
      max-width: 1600px;
      margin: 0 auto;
      padding: 0 40px;
      display: grid;
      grid-template-columns: 2fr 1fr 1fr;
      gap: 40px;
    }

    .footer-logo {
      font-size: 28px;
      font-weight: 800;
      background: linear-gradient(90deg, #00F5D4, #00C3FF);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      margin-bottom: 20px;
      display: inline-block;
    }

    .footer-description {
      color: var(--text-light);
      font-size: 0.95rem;
      line-height: 1.6;
      max-width: 500px;
      margin-bottom: 20px;
    }

    .footer-heading {
      font-size: 1.3rem;
      margin-bottom: 20px;
      color: var(--text);
      position: relative;
      padding-bottom: 10px;
    }

    .footer-heading::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 40px;
      height: 3px;
      background: linear-gradient(90deg, var(--accent), #00C3FF);
      border-radius: 2px;
    }

    .footer-links {
      list-style: none;
    }

    .footer-links li {
      margin-bottom: 15px;
    }

    .footer-links a {
      color: var(--text-light);
      text-decoration: none;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 0.95rem;
    }

    .footer-links a:hover {
      color: var(--accent);
      transform: translateX(5px);
    }

    .footer-links i {
      color: var(--accent);
      font-size: 0.8rem;
    }

    .social-links {
      display: flex;
      gap: 20px;
      margin-top: 20px;
    }

    .social-links a {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background: rgba(0, 245, 212, 0.1);
      color: var(--accent);
      font-size: 1.1rem;
      transition: all 0.3s ease;
      text-decoration: none;
    }

    .social-links a:hover {
      background: var(--accent);
      color: var(--bg-dark);
      transform: translateY(-3px);
    }

    .copyright {
      text-align: center;
      padding-top: 30px;
      margin-top: 30px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      color: var(--text-light);
      font-size: 0.9rem;
      line-height: 1.5;
    }

    .copyright span {
      color: var(--accent);
      font-weight: 600;
    }

    /* ============ 响应式设计 ============ */
    @media (max-width: 1400px) {
      .container {
        max-width: 1400px;
      }
      
      .footer-container {
        max-width: 1400px;
      }
      
      .cards {
        grid-template-columns: repeat(2, 1fr);
        gap: 25px;
      }
      
      .card {
        padding: 25px;
      }
    }

    @media (max-width: 1200px) {
      .container {
        max-width: 1200px;
        padding: 0 30px;
      }
      
      .footer-container {
        max-width: 1200px;
        padding: 0 30px;
        grid-template-columns: 1fr 1fr;
      }
      
      .hero h1 {
        font-size: 2.5rem;
      }
      
      .cards {
        grid-template-columns: repeat(2, 1fr);
      }
      
      .morning-star {
        left: 10%;
      }
    }

    @media (max-width: 992px) {
      .container {
        padding: 0 20px;
      }
      
      nav ul {
        gap: 20px;
      }
      
      nav a {
        font-size: 1.1rem;
      }
      
      .hero h1 {
        font-size: 2.2rem;
      }
      
      .hero p {
        font-size: 1.1rem;
      }
      
      .cards {
        grid-template-columns: 1fr;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
      }
      
      .card {
        padding: 25px;
      }
      
      .btn {
        min-width: 100%;
        padding: 14px 30px;
      }
      
      .morning-star {
        top: 10%;
        left: 5%;
      }
    }

    @media (max-width: 768px) {
      .hero h1 {
        font-size: 1.8rem;
      }
      
      nav ul {
        display: none;
      }
      
      .logo {
        font-size: 28px;
      }
      
      .footer-container {
        grid-template-columns: 1fr;
        gap: 30px;
      }
      
      .card {
        padding: 20px;
      }
      
      .card h3 {
        font-size: 1.4rem;
      }
      
      .quick-access h2 {
        font-size: 1.6rem;
      }
      
      .buttons {
        gap: 15px;
      }
      
      .icons {
        gap: 15px;
        font-size: 20px;
      }
      
      .morning-star {
        width: 8px;
        height: 8px;
        top: 8%;
      }
      
      .star-big {
        width: 6px;
        height: 6px;
      }
      
      .star-medium {
        width: 4px;
        height: 4px;
      }
      
      .star-small {
        width: 2px;
        height: 2px;
      }
    }

    @media (max-width: 480px) {
      .hero h1 {
        font-size: 1.6rem;
      }
      
      .container {
        padding: 0 15px;
      }
      
      .footer-container {
        padding: 0 15px;
      }
      
      .card {
        padding: 18px;
      }
      
      .card h3 {
        font-size: 1.3rem;
      }
      
      .quick-access h2 {
        font-size: 1.4rem;
      }
      
      .buttons {
        gap: 12px;
      }
      
      .morning-star {
        width: 6px;
        height: 6px;
      }
    }
  </style>
</head>
<body>
  <!-- 星星容器 -->
  <div class="stars-container" id="stars-container">
    <!-- 启明星（最亮的那颗） -->
    <div class="morning-star"></div>
  </div>
  
  <div class="container">
    <!-- 头部 -->
    <header>
      <a href="数学宇宙.html" class="logo">MathVerse</a>
      <nav>
        <ul>
          <li><a href="数学宇宙.html">首页</a></li>
          <li><a href="知识图谱.html">知识图谱</a></li>
          <li><a href="智慧工坊.html">智慧工坊</a></li>
          <li><a href="智能题库.html">智能题库</a></li>
          <li><a href="备考冲刺.html">备考冲刺</a></li>
          <li><a href="学习社区.html">学习社区</a></li>
        </ul>
      </nav>
      <div class="icons">
        <!-- 搜索图标 -->
        <button id="search-toggle" class="icon-btn">
          <i class="fas fa-search"></i>
        </button>

        <!-- 夜间模式切换 -->
        <button id="theme-toggle" class="icon-btn">
          <i class="fas fa-moon"></i>
        </button>

        <!-- 用户账户 -->
        <button id="user-toggle" class="icon-btn">
          <i class="fas fa-user"></i>
        </button>
      </div>

      <!-- 搜索弹窗 -->
      <div id="search-modal" class="modal">
        <div class="modal-content">
          <span class="close">&times;</span>
          <form action="/search" method="GET">
            <input type="text" name="q" placeholder="搜索数学知识点、题目..." required />
            <button type="submit">搜索</button>
          </form>
        </div>
      </div>

      <!-- 用户登录/注册弹窗 -->
      <div id="user-modal" class="modal">
        <div class="modal-content">
          <span class="close">&times;</span>
          <h3>登录 / 注册</h3>
          <form id="login-form">
            <input type="text" name="username" placeholder="用户名" required />
            <input type="password" name="password" placeholder="密码" required />
            <button type="submit">登录</button>
            <p>没有账号？<a href="#" id="switch-to-register">立即注册</a></p>
          </form>
          <form id="register-form" style="display: none;">
            <input type="text" name="username" placeholder="用户名" required />
            <input type="email" name="email" placeholder="邮箱" required />
            <input type="password" name="password" placeholder="密码" required />
            <button type="submit">注册</button>
            <p>已有账号？<a href="#" id="switch-to-login">返回登录</a></p>
          </form>
        </div>
      </div>
    </header>

    <!-- 主内容 -->
    <main>
      <section class="hero">
        <h1>星光不问赶路人，时光不负有心人 ✨</h1>
        <p>探索高中数学的浩瀚宇宙，在函数、几何与概率的星辰中找到你的轨道</p>
        <p>人类群星闪耀时，数学光芒永不灭</p>
      </section>

      <!-- 功能卡片 -->
      <section class="cards">
        <!-- 知识图谱 -->
        <a href="知识图谱.html" class="card">
          <div class="card-icon"><i class="fas fa-project-diagram"></i></div>
          <h3>知识图谱</h3>
          <p>3D交互式知识星系，点击星球深入每个公式与定理，构建你的数学知识网络。</p>
        </a>
        
        <!-- 解题工坊 -->
        <a href="智慧工坊.html" class="card">
          <div class="card-icon"><i class="fas fa-tools"></i></div>
          <h3>智慧工坊</h3>
          <p>GeoGebra绘图、手写识别、公式速查一应俱全，为每一道难题提供智能解题工具。</p>
        </a>
        
        <!-- 智能题库 -->
        <a href="智能题库.html" class="card">
          <div class="card-icon"><i class="fas fa-calculator"></i></div>
          <h3>智能题库</h3>
          <p>百万真题+AI推荐，精准攻克薄弱环节，个性化题目推荐，高效提升数学能力。</p>
        </a>

        <!-- 高考冲刺 -->
        <a href="备考冲刺.html" class="card">
          <div class="card-icon"><i class="fas fa-graduation-cap"></i></div>
          <h3>备考冲刺</h3>
          <p>近10年真题+押题预测，模拟考试与详细解析，助你冲刺985、211名校。</p>
        </a>
      </section>

      <!-- 快捷入口 -->
      <section class="quick-access">
        <h2>🚀 今日快捷入口</h2>
        <div class="buttons">
          <a href="智能题库.html" class="btn">
            <i class="fas fa-play-circle"></i> 今日练习
          </a>
          <a href="备考冲刺.html" class="btn">
            <i class="fas fa-book"></i> 模拟测试
          </a>
          <a href="轻松一刻.html" class="btn">
            <i class="fas fa-gamepad"></i> 轻松一刻
          </a>
        </div>
      </section>
    </main>
  </div>

  <!-- 页脚 -->
  <footer>
    <div class="footer-container">
      <div class="footer-info">
        <div class="footer-logo">MathVerse</div>
        <p class="footer-description">
          MathVerse是一个专为高中生设计的数学学习平台，旨在通过智能化的工具和丰富的学习资源，帮助学生更好地理解和掌握高中数学知识，提升数学思维能力。
        </p>
        <div class="social-links">
          <a href="https://weixin.qq.com/"><i class="fab fa-weixin"></i></a>
          <a href="https://im.qq.com/index/"><i class="fab fa-qq"></i></a>
          <a href="https://weibo.com/"><i class="fab fa-weibo"></i></a>
          <a href="https://github.com/apache/echarts"><i class="fab fa-github"></i></a>
        </div>
      </div>
      
      <div class="footer-section">
        <h3 class="footer-heading">快速链接</h3>
        <ul class="footer-links">
          <li><a href="数学宇宙.html"><i class="fas fa-chevron-right"></i> 首页</a></li>
          <li><a href="知识图谱.html"><i class="fas fa-chevron-right"></i> 知识图谱</a></li>
          <li><a href="智慧工坊.html"><i class="fas fa-chevron-right"></i> 智慧工坊</a></li>
          <li><a href="智能题库.html"><i class="fas fa-chevron-right"></i> 智能题库</a></li>
        </ul>
      </div>
      
      <div class="footer-section">
        <h3 class="footer-heading">支持与帮助</h3>
        <ul class="footer-links">
          <li><a href="http://www.qingcloud.website/"><i class="fas fa-chevron-right" target="_blank"></i> 帮助中心</a></li>
          <li><a href="http://www.qingcloud.website/"><i class="fas fa-chevron-right" target="_blank"></i> 常见问题</a></li>
          <li><a href="http://www.qingcloud.website/"><i class="fas fa-chevron-right" target="_blank"></i> 联系我们</a></li>
          <li><a href="http://www.qingcloud.website/"><i class="fas fa-chevron-right" target="_blank"></i> 隐私政策</a></li>
        </ul>
      </div>
    </div>
    
    <div class="copyright">
      &copy; 2025 <span>MathVerse</span> - 探索高中数学的浩瀚宇宙. 人类群星闪耀时，数学光芒永不灭.
    </div>
  </footer>

  <script>
    // 星星生成函数
    function createStars() {
      const container = document.getElementById('stars-container');
      
      // 星星数量配置
      const starConfig = {
        big: 8,
        medium: 25,
        small: 50,
        color: 15,
        shooting: 3
      };
      
      // 创建大星星
      for (let i = 0; i < starConfig.big; i++) {
        const star = document.createElement('div');
        star.className = 'star-big';
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 2 + 's';
        container.appendChild(star);
      }
      
      // 创建中星星
      for (let i = 0; i < starConfig.medium; i++) {
        const star = document.createElement('div');
        star.className = 'star-medium';
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 1.8 + 's';
        star.style.animationDuration = Math.random() * 0.5 + 1.5 + 's';
        container.appendChild(star);
      }
      
      // 创建小星星
      for (let i = 0; i < starConfig.small; i++) {
        const star = document.createElement('div');
        star.className = 'star-small';
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 1.5 + 's';
        star.style.animationDuration = Math.random() * 0.3 + 1.2 + 's';
        container.appendChild(star);
      }
      
      // 创建彩色星星
      const colors = ['blue', 'purple', 'pink'];
      for (let i = 0; i < starConfig.color; i++) {
        const star = document.createElement('div');
        star.className = 'star-color ' + colors[Math.floor(Math.random() * colors.length)];
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.animationDelay = Math.random() * 2.5 + 's';
        container.appendChild(star);
      }
      
      
    }
    
    // 页面加载完成后创建星星
    document.addEventListener('DOMContentLoaded', createStars);
    
    // 窗口大小改变时重新调整启明星位置
    window.addEventListener('resize', function() {
      const morningStar = document.querySelector('.morning-star');
      if (window.innerWidth < 992) {
        morningStar.style.left = '5%';
        morningStar.style.top = '10%';
      } else if (window.innerWidth < 1200) {
        morningStar.style.left = '10%';
        morningStar.style.top = '15%';
      } else {
        morningStar.style.left = '15%';
        morningStar.style.top = '15%';
      }
    });
    
    // 搜索弹窗控制
    const searchToggle = document.getElementById('search-toggle');
    const searchModal = document.getElementById('search-modal');
    const closeSearch = searchModal.querySelector('.close');

    searchToggle.addEventListener('click', () => {
      searchModal.style.display = 'block';
    });

    closeSearch.addEventListener('click', () => {
      searchModal.style.display = 'none';
    });

    window.addEventListener('click', (e) => {
      if (e.target === searchModal) {
        searchModal.style.display = 'none';
      }
    });

    // 主题切换
    const themeToggle = document.getElementById('theme-toggle');
    const body = document.body;

    themeToggle.addEventListener('click', () => {
      if (body.classList.contains('light-theme')) {
        body.classList.remove('light-theme');
        themeToggle.innerHTML = '<i class="fas fa-moon"></i>';
      } else {
        body.classList.add('light-theme');
        themeToggle.innerHTML = '<i class="fas fa-sun"></i>';
      }
    });

    // 用户弹窗控制
    const userToggle = document.getElementById('user-toggle');
    const userModal = document.getElementById('user-modal');
    const closeUser = userModal.querySelector('.close');

    userToggle.addEventListener('click', () => {
      userModal.style.display = 'block';
    });

    closeUser.addEventListener('click', () => {
      userModal.style.display = 'none';
    });

    window.addEventListener('click', (e) => {
      if (e.target === userModal) {
        userModal.style.display = 'none';
      }
    });

    // 登录/注册切换
    document.getElementById('switch-to-register').addEventListener('click', (e) => {
      e.preventDefault();
      document.getElementById('login-form').style.display = 'none';
      document.getElementById('register-form').style.display = 'block';
    });

    document.getElementById('switch-to-login').addEventListener('click', (e) => {
      e.preventDefault();
      document.getElementById('register-form').style.display = 'none';
      document.getElementById('login-form').style.display = 'block';
    });
    
  </script>
</body>
</html>